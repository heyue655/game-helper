---
description: 强制 UTF-8 编码规范，防止中文内容被破坏
alwaysApply: true
---

# UTF-8 编码与中文内容保护规则

## 核心原则

本项目所有文件使用 UTF-8 编码，所有用户可见文本使用中文。

## 编辑含中文的文件

1. **必须使用 Write 工具整体重写文件**，禁止使用 StrReplace 或 ApplyPatch 对含中文内容的文件做局部替换（局部替换会破坏多字节 UTF-8 字符）
2. 每次编辑含中文的文件后，**立即用 Read 工具回读**，确认中文内容未被破坏
3. 如果回读发现乱码（`????`、mojibake 如 `å·²ä¸æ¶`、unicode 转义如 `\u4e2d`），必须立即用 Write 重写修复

## 禁止事项

- 禁止在代码中使用 unicode 转义序列（如 `\u4e2d\u6587`）代替真正的中文字符
- 禁止出现 `????` 问号替代中文
- 禁止出现 mojibake 乱码（如 `å·²ä¸æ¶`、`ååç®¡ç`）
- 禁止将中文替换为英文

## 新建文件

新建含中文的文件时，使用 Write 工具，直接在内容中写入正确的 UTF-8 中文字符。
